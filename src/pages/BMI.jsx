import { MainLayout } from "../layouts/MainLayout";
import BackButton from "@components/UI/BackButton";

export const BMI = () => {
  return (
    <MainLayout>
      This is a BMI page <BackButton />
    </MainLayout>
  );
};
